// 나의 정보
function my_info() {
  const name    = "전정빈";
  const age     = 25;
  const phone   = "010-1234-5678";
  const weight  = 70.5;

  alert(
    `내 정보
이름       : ${name}
나이       : ${age}세
전화번호   : ${phone}
몸무게     : ${weight}kg`
  );
}

// 형제의 정보
function brother_info() {
  const name    = "전길동";
  const age     = 27;
  const phone   = "010-8765-4321";
  const weight  = 68.2;

  alert(
    `형제 정보
이름       : ${name}
나이       : ${age}세
전화번호   : ${phone}
몸무게     : ${weight}kg`
  );
}

// 부모의 정보
function parent_info() {
  const name    = "홍철수";
  const age     = 55;
  const phone   = "02-123-4567";
  const weight  = 72.3;

  alert(
    `부모 정보
이름       : ${name}
나이       : ${age}세
전화번호   : ${phone}
몸무게     : ${weight}kg`
  );
}

